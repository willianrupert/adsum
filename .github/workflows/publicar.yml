# Publica o app no GitHub Pages a cada push na branch principal.
#
# Em Settings → Pages, escolha "GitHub Actions" como origem. O `base` do Vite
# precisa casar com o nome do repositório — ver `vite.config.ts`.

name: publicar

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  construir:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      # Publicar sem passar nos testes seria publicar sem saber. Os testes de
      # `nucleo/csv.ts` são o que garante que o app ainda conversa com o cartão.
      - run: npm test
      - run: npm run build
        env:
          BASE_ADSUM: /${{ github.event.repository.name }}/
      - uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  publicar:
    needs: construir
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        uses: actions/deploy-pages@v5
